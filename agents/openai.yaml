interface:
  display_name: "目标全路径分析"
  short_description: "区分改功能、修 bug、新系统，按 P/M/T 控范围"
  default_prompt: "Use $goal-path-analysis 先判断是已有项目变更、Bug 修复还是从 0 新系统；改功能按六阶段找证据和 P/M/T，修 bug 按五阶段先复现再定根因，新系统按七阶段定 MVP、设计路径、定方案并单点开发验收。"
policy:
  products:
    - "chatgpt"
    - "codex"
    - "api"
    - "atlas"
  allow_implicit_invocation: true
