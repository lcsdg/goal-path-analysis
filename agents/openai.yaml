interface:
  display_name: "目标全路径分析"
  short_description: "七类开发场景按 P/M/T 控范围和验证"
  default_prompt: "Use $goal-path-analysis 先判断场景：线上故障、Bug 修复、联调/接口对接、性能优化、重构、从 0 新系统或已有项目变更；按对应流程先分析/设计和 P/M/T，确认后单点实施，再基于 diff/证据 review 并输出验证清单。"
policy:
  products:
    - "chatgpt"
    - "codex"
    - "api"
    - "atlas"
  allow_implicit_invocation: true
