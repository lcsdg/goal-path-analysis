interface:
  display_name: "目标全路径分析"
  short_description: "反查完整路径，约束先分析、再实施、再验证"
  default_prompt: "Use $goal-path-analysis 先复述需求和验收标准，找全目标路径与影响面；未获明确授权时只分析，授权实施后按 P/M/T 账本小步修改、review 并输出测试清单。"
policy:
  products:
    - "chatgpt"
    - "codex"
    - "api"
    - "atlas"
  allow_implicit_invocation: true
